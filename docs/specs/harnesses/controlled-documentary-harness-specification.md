## Harness Specification Title

`controlled-documentary-harness`

## Documentary Status

`DOCUMENTARY_HARNESS_SPECIFICATION_ONLY`

## Purpose

This specification describes only, at a documentary and conceptual level, how a future harness could coordinate spec reading, scope validation, authorized path inspection, governance violation detection, evidence recording, and translation by adapters without executing anything.

## Institutional Principle

`Spec is authority. Institutional skill is capability. Adapter is translation. Executor is replaceable.`

## Harness Nature

The `controlled-documentary-harness` is only a documentary harness specification.

It is not an executable harness.

It is not a pipeline.

It is not a workflow.

It is not a runner.

It is not a registry.

It does not call commands.

It does not call subagents.

It does not call adapters.

It does not call executors.

It does not create files by itself.

It does not modify files by itself.

## Related Institutional Skills

1. `read-approved-specs`
2. `validate-scope-boundaries`
3. `inspect-authorized-paths`
4. `detect-governance-violation`
5. `write-evidence-record`

The harness does not replace any skill.

## Related Controlled Subagents

1. `.claude/agents/spec-reader-subagent.md`
2. `.claude/agents/scope-validator-subagent.md`
3. `.claude/agents/path-inspector-subagent.md`
4. `.claude/agents/governance-violation-detector-subagent.md`
5. `.claude/agents/evidence-recorder-subagent.md`

The harness does not execute these subagents.

## Related Controlled Commands

1. `.claude/commands/read-approved-specs.md`
2. `.claude/commands/validate-scope-boundaries.md`
3. `.claude/commands/inspect-authorized-paths.md`
4. `.claude/commands/detect-governance-violation.md`
5. `.claude/commands/write-evidence-record.md`

The harness does not execute these commands.

## Related Controlled Adapter Specifications

1. `/docs/specs/adapters/claude-code-adapter-specification.md`
2. `/docs/specs/adapters/codex-adapter-specification.md`
3. `/docs/specs/adapters/human-review-adapter-specification.md`
4. `/docs/specs/adapters/evidence-log-adapter-specification.md`
5. `/docs/specs/adapters/spec-ingestion-adapter-specification.md`

The controlled adapter specifications index is:

`/docs/specs/adapters/CONTROLLED_ADAPTER_SPECIFICATIONS_INDEX.md`

The harness does not execute adapters.

The harness does not create an adapter registry.

The harness does not create a runner.

## Conceptual Coordination Responsibility

The conceptual responsibility of the `controlled-documentary-harness` is to document, in a future authorized phase, the safe coordination order between:

1. reading approved specs;
2. validating scope;
3. inspecting authorized paths;
4. detecting governance violations;
5. recording evidence;
6. preserving negative boundaries;
7. translating through documentary adapters;
8. requiring explicit human authorization;
9. confirming no automatic execution.

This coordination is only documentary in this task.

No step is executed.

No command, subagent, adapter, or executor call is performed.

## Allowed Documentary Inputs

This specification allows only conceptual and documentary inputs:

* approved specs;
* institutional skills;
* existing controlled subagents;
* existing controlled commands;
* existing controlled adapter specifications;
* current readiness statement;
* explicit human authorization;
* authorized artifacts;
* authorized paths;
* negative scope;
* forbidden artifacts;
* evidence records;
* governance stop conditions;
* future task gates;
* confirmations of no execution.

## Expected Documentary Outputs

A future harness, if separately authorized, could only produce documentary outputs such as:

* documentary coordination sequence;
* authorized scope observed;
* authorized paths observed;
* authorized artifacts observed;
* negative boundaries preserved;
* documentary violations detected;
* expected evidence;
* expected readiness statement;
* future task gate observed;
* confirmation of no automatic execution.

This must not be turned into a schema. It uses no YAML. It uses no JSON. It creates no machine-readable contract.

## Spec Authority Boundary

The harness does not decide authority.

The spec remains authority.

The harness cannot reinterpret approved specs.

The harness cannot alter approved specs.

## Skill Capability Boundary

Skills are documentary institutional capabilities.

The harness does not replace skills.

The harness does not create new skills.

The harness does not execute skills.

## Adapter Translation Boundary

Adapters are translation.

The harness does not replace adapters.

The harness does not create adapters.

The harness does not execute adapters.

The harness does not transform a documentary adapter into an executable adapter.

## Executor Replaceability Boundary

Executors are replaceable.

The harness does not depend on a specific executor.

The harness does not call Claude, Claude Code, Codex, or external tools.

The harness does not create operational coupling with an executor.

## Human Authorization Boundary

The harness cannot be used as human authorization.

Any future use will require its own task, exact artifact, and explicit human authorization.

The following insufficient phrases remain insufficient:

* vamos
* segue
* manda
* próximo
* ok
* aprovado
* pode continuar
* faça
* sim
* bora
* continue

## Evidence Expectations

Any future use related to the harness must preserve documentary evidence about:

* source documents consulted;
* readiness statement observed;
* explicit human authorization observed;
* authorized scope;
* authorized artifacts;
* authorized paths;
* negative boundaries;
* reported created files;
* reported modified files;
* commands not executed;
* subagents not executed;
* adapters not executed;
* executors not called;
* confirmation of no execution;
* confirmation that no next task was automatically authorized.

## Governance Stop Conditions

Any future use related to the harness must recommend stopping if there is:

* absent or incompatible readiness statement;
* explicit human authorization absent when required;
* insufficient phrase being treated as authorization;
* attempt to create executable harness;
* attempt to create runner;
* attempt to create registry;
* attempt to create executable pipeline;
* attempt to create executable workflow;
* attempt to create YAML;
* attempt to create JSON;
* attempt to create a machine-readable contract;
* attempt to execute command;
* attempt to execute subagent;
* attempt to execute adapter;
* attempt to call executor;
* attempt to alter approved specs;
* attempt to create code;
* attempt to create API;
* attempt to create schema;
* attempt to create migration;
* attempt to create frontend;
* attempt to start a next task automatically;
* break in the separation between spec, skill, adapter, and executor.

## Strict Boundaries

This documentary harness:

* does not execute;
* does not coordinate execution;
* does not call commands;
* does not call subagents;
* does not call adapters;
* does not call executors;
* does not create a runner;
* does not create a registry;
* does not create a pipeline;
* does not create a workflow;
* does not create YAML;
* does not create JSON;
* does not create a machine-readable contract;
* does not create code;
* does not create API;
* does not create schema;
* does not create migration;
* does not create frontend;
* does not create backlog;
* does not create sprint plan;
* does not create roadmap;
* does not authorize a next task.

## Forbidden Actions

The following are forbidden:

* creating executable harness;
* creating technical harness;
* creating runner;
* creating registry;
* creating executable pipeline;
* creating executable workflow;
* creating executable detector;
* creating executable logger;
* creating executable validator;
* creating YAML;
* creating JSON;
* creating a machine-readable contract;
* creating code;
* creating API;
* creating schema;
* creating migration;
* creating frontend;
* creating new adapter;
* creating new command;
* creating new subagent;
* executing command;
* executing subagent;
* executing adapter;
* executing Claude;
* executing Claude Code;
* executing Codex;
* executing external tool;
* altering approved specs;
* altering P0P4 packages;
* altering P0P4 records;
* starting the next task automatically.

## Non-Executable Declaration

`controlled-documentary-harness` is only a documentary specification.

* No executable harness was created.
* No technical harness was created.
* No runner was created.
* No registry was created.
* No executable pipeline was created.
* No executable workflow was created.
* No YAML was created.
* No JSON was created.
* No machine-readable contract was created.
* No code was created.
* No execution was performed.

## Future Implementation Boundary

This specification does not authorize future implementation.

Any future executable harness will require:

* its own task;
* explicit human authorization;
* exact artifact;
* negative scope;
* documentary evidence;
* stop conditions;
* confirmation of no automatic execution;
* confirmation that specs remain authority;
* confirmation that skills remain capabilities;
* confirmation that adapters remain translation;
* confirmation that executors remain replaceable.

## Future Task Gate

Task 149 does not authorize Task 150 automatically.

The next candidate task is only:

`Task 150  Create Controlled Harness Boundary Specification`

Status:

`NOT_AUTHORIZED_AUTOMATICALLY`

The required future phrase is:

`EU AUTORIZO A TASK 150 PARA CRIAR A ESPECIFICAÇÃO DOCUMENTAL CONTROLADA DE BOUNDARIES DO HARNESS, SEM HARNESS EXECUTÁVEL E SEM EXECUÇÃO.`

## Readiness Statement

`TASK_149_FIRST_CONTROLLED_HARNESS_SPECIFICATION_CREATED_DOCUMENTARY_ONLY`
