"use client";

export function GrowthLabel({ children }: { children: React.ReactNode }) {
  return <span className="text-[0.68rem] uppercase tracking-[0.14em] text-[var(--yzi-text-faint)]">{children}</span>;
}

